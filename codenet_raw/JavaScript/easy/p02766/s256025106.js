function Main(input) {
  input = input.split(" ");
  const bs = parseInt(arr[0]).toString(parseInt(arr[1]));
  console.log(bs.toString().length);
}
 
//*この行以降は編集しないでください（標準入出力から一度に読み込み、Mainを呼び出します）
Main(require("fs").readFileSync("/dev/stdin", "utf8"));