//a
const solve = (input) => {
  input = input.split("\n");
  const nums = input[0].split(' ').map(e=>parseInt(e));
  console.log('%d', Math.min(c,Math.floor(b/a)));
}

solve(require("fs").readFileSync("/dev/stdin", "utf8"));
